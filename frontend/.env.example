# Copy to .env.local and fill in values for your environment.
# In development, leave VITE_API_BASE_URL empty — Vite proxies /api to localhost:8080.
# In production, set it to your deployed gateway URL.
VITE_API_BASE_URL=

# Google Maps JavaScript API key (admin map location picker)
VITE_GOOGLE_MAPS_API_KEY=
