http.host: "0.0.0.0"
xpack.monitoring.enabled: false

# Pipeline settings
pipeline.workers: 2
pipeline.batch.size: 125
pipeline.batch.delay: 50

# Aggregate filter needs persistent storage for the join map
# In production, replace with a Redis-backed store for multi-node deployments
path.data: /usr/share/logstash/data
