package com.foodzie.delivery_matching_service.data;

/**
 * Mirror of delivery-service VehicleType — kept local to avoid cross-service dependency.
 */
public enum VehicleType {
    BIKE,
    SCOOTER,
    BICYCLE
}
