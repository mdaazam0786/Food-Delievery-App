spring.application.name=delivery-acceptance-service
server.port=8095

# Eureka
eureka.client.service-url.defaultZone=https://food-delievery-app-eureka-server.onrender.com/eureka/
eureka.instance.hostname=${DELIVERY_ACCEPTANCE_SERVICE_HOST:food-delievery-app-delivery-acceptance-service.onrender.com}
eureka.instance.secure-port=443
eureka.instance.secure-port-enabled=true
eureka.instance.non-secure-port-enabled=false

# MySQL — driver DB (shared with delivery-service and delivery-matching-service)
spring.datasource.url=jdbc:mysql://thomas.proxy.rlwy.net:27174/foodzie
spring.datasource.username=root
spring.datasource.password=yhWvanlmaBlHndJzHPoIsCOPHpfeMpqI
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Redis
spring.data.redis.host=${SPRING_DATA_REDIS_HOST}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT}
spring.data.redis.username=${SPRING_DATA_REDIS_USERNAME}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD}

# Kafka
spring.kafka.bootstrap-servers=${RAILWAY_TCP_PROXY_DOMAIN}:${RAILWAY_TCP_PROXY_PORT}
spring.kafka.security.protocol=SASL_PLAINTEXT
spring.kafka.sasl.mechanism=PLAIN
spring.kafka.sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username="kafka" password="IDFLF22yXTBRd4x9b84_FlN4d-OKtuhu";
spring.kafka.consumer.group-id=delivery-acceptance-service
spring.kafka.consumer.auto-offset-reset=earliest

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# JWT
app.jwt.secret=ZmVlZGllLXVzZXItc2VydmljZS1zZWNyZXQta2V5LTI1Ni1iaXRzLWxvbmch

# CORS
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,https://food-delievery-app-b6b3.vercel.app

# Kafka topics
app.kafka.topics.delivery-events=delivery-events
app.kafka.topics.order-events=order-events

# Kafka Auto-create Topics
spring.kafka.admin.properties.bootstrap.servers=thomas.proxy.rlwy.net:19955

# Offer TTL — how long drivers have to accept before the offer expires
# and the matching service is asked to widen the search radius
app.acceptance.offer-ttl-seconds=30
